# Blender lyrics generator

- `blender/generate_lyrics.py` - the main script that reads lyrics and generates them in Blender's
  scene as text objects.
- `example.json` - example json that shows the format.
